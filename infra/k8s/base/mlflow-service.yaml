apiVersion: v1
kind: Service
metadata:
  name: automl-mlflow
  namespace: automl
spec:
  selector:
    app.kubernetes.io/name: automl-mlflow
  ports:
    - name: http
      port: 5000
      targetPort: http
