apiVersion: v1
kind: Service
metadata:
  name: automl-seaweedfs
  namespace: automl
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: automl-seaweedfs
  ports:
    - name: s3
      port: 8333
      targetPort: s3
